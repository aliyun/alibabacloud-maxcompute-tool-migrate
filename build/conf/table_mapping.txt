# The following example represents a table migration job. The source table is 'source_table'
in database 'source_db' and the destination table is 'dest_table' in MaxCompute project 'dest_pjt'

source_db.source_table:dest_pjt.dest_table

# The following example represents a partition
